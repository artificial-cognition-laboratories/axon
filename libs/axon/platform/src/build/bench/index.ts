// bench — native Bun experiments over agents, models, cognets, and values.
//
// A bench is a project kind: finding, opening, scaffolding, preparing and
// publishing one are Project()'s, like every other kind. What lives here is
// the benchmarking itself — the verbs Project hangs off `project.bench`.

export { Bench, type BenchT, type BenchExtras } from "./bench"
