// modules — local source modules hard-imported by an agent's config.
// SourceModules() is the module's single entry point.

export { SourceModules, type SourceModulesT, type SourceModule, type LinkResult } from "./modules"
export type { LockT, LockFile, LockEntry } from "./lock"
